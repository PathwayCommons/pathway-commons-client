/**
 * Peforms a GET web query to the Pathway Commons web service
 * @module get
 * @param {array} query_array
 * @param {requestCallback} callback - getResponseObject
 */
const get = (query_array, callback) => {
	callback(responseText);
}

export default get;
